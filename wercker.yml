box: debian
build:
  steps:
    - arjen/hugo-build:
        version: "0.50"
deploy-firebase:
  steps:
    - devillexio/firebase-deploy@1.2.0:
        project: $FIREBASE_PROJECT_NAME
        token: $FIREBASE_TOKEN